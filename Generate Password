[4:59 pm, 16/7/2024] Niharika M.Tech: import random
import string

def generate_password(length):
    # Define the character pool: lowercase, uppercase, digits, and punctuation
    characters = string.ascii_letters + string.digits + string.punctuation
    # Randomly choose characters from the pool
    password = ''.join(random.choices(characters, k=length))
    return password

def main():
    # Prompt user for the desired length of the password
    length = int(input("Enter the desired length for the password: "))
    
    # Generate the password
    password = generate_password(length)
    
    # Display the generated password
    print(f"Generated password: {password}")

if _name_ == "_main_":
    main()
[4:59 pm, 16/7/2024] Niharika M.Tech: Generate password
